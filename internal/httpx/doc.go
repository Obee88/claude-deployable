// Package httpx is a small collection of HTTP helpers used by the
// VPS agent (M3): JSON error shapes, structured request logging, and
// lightweight middleware composition.
//
// Scaffold only in M1 task 8; implementation arrives in M3 alongside
// the VPS agent itself.
package httpx
