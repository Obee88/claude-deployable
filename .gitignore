# Sandbox-native git config — contains GH_PAT.  Lives at the repo root
# by design (see SETUP.md and PLAN.md security section).  The committed
# reference is .env.example; the working .env must never be committed.
.env
.env.local
.env.*.local

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
*.swp
*~
